@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model IEnumerable<WebCrawler.Models.User_Message>
@{
}
<div class="card">
    <div class="card-header">
        <div class=row>
            
            <div class="col-8">
              <h5>公告</h5>
            </div>
        
            <div class="col-4 justify-content-end">
              <a class="btn btn-primary" asp-action="Message_Create">新增訊息</a>
            </div>
        </div>
    </div>
    <div class="card-body">
        
        @*<div class="row mb-3">
             <div class="col-3"><h5>標題</h5></div>
             <div class="col-5"><h5>內容</h5></div>
             <div class="col-4"><h5>選項</h5></div>
        </div>*@
        <table class="table">
            <thead>
                <th scope="col">使用者/Email</th>
                <th scope="col">公佈日期</th>
                <th scope="col">選項</th>
            </thead>
            <tbody>
                @foreach(var i in Model)
                {
                    <tr>
                        <th scope="row">
                            @Html.DisplayFor(model => i.UName)/@Html.DisplayFor(model => i.UEmail)
                        </th>
                        <td>
                            <span class="d-inline-block text-truncate" style="max-width: 390px;">@Html.DisplayFor(model => i.Date)</span>
                        </td>
                        <td>
                            <a role="button" class="btn btn-success barbtn" asp-action="Message_Edit" asp-route-id="@i.MesId">編輯</a>&emsp;&emsp;
                            <a role="button" class="btn btn-danger barbtn" asp-action="Message_del" asp-route-id="@i.MesId" onclick="return confirm('Are you sure you want to delete this?')" >刪除</a>
                        </td>
                    </tr>
                    @*<div class="row mb-3">
                        <div class="col-3"><h5>@Html.DisplayFor(model => i.Title)</h5></div>
                        <div class="col-5"><span class="d-inline-block text-truncate" style="max-width: 390px;">@Html.DisplayFor(model => i.Content)</span></div>
                        <div class="col-4 btn-group justify-content-center">
                            <a role="button" class="btn btn-success barbtn" asp-action="Announment_Edit" asp-route-id="@i.AnnoId">編輯</a>&emsp;&emsp;
                            <a role="button" class="btn btn-danger barbtn" asp-action="Announment_del" asp-route-id="@i.AnnoId" onclick="return confirm('Are you sure you want to delete this?')" >刪除</a>
                        </div>
                    </div> *@          
                }
            </tbody>
        </table>
    </div>
</div>